class User {
  String name, email, phone, radius, credit, rating, date;

  User(
      {this.name,
      this.email,
      this.phone,
      this.radius,
      this.credit,
      this.rating,
      this.date});
}
